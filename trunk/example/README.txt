This is a complete project that provides the following data flow:

Table CATEGORIES -> CatDAO+CatEntity -> CatModel -> CatService ->
-> CatServlet -> categoryPage.jsp -> browser

It could be ran as an Eclipse project or WAR file as well,
using Tomcat

It looks ugly but works

For detailes see "step-by-step-report.txt"